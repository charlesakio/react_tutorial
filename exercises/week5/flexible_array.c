#include <stdio.h>
#include <stdlib.h>

//Read the number of integers to be entreed
	//Get input from user
	//Check number of intergers


//Allocate the memory to the pointer arrPtr to hold arrSize integers 


//Read arrSize integers and store them into the allocated memory.
//The integers are read as string and converted into integers[fgets(myStr, 20 stdin); arrPtr[k]= atoi(mystr)]


//Print the sum of the integers entered
int main(){
	int userInput = 0;
	
	printf("How many integers would you like to have?");
	scanf("%d", &userInput);
	printf("User input is: %d", userInput);

}//main
